# System Design: Repo Release Notifier

## 1. Вимоги системи

### Функціональні вимоги

- Користувачі можуть підписатися на реліз нових версій GitHub репозиторіїв
- Система повинна надсилати сповіщення про новий реліз, якщо такий існує
- Користувач може відписатися від репозиторію
- Користувач може переглянути усі свої активні підписки
- Система повинна сканувати усі репозиторії на наявність нових релізів в певний проміжок часу(визначається розробником)

### Нефункціональні вимоги

- **Доступність**: 99.9% uptime
- **Масштабованість**: до 10 користувачів, 100 повідомлень/день
- **Безпека**: Надання відповіді лише за наявності API ключа(якщо він ініціалізований)

### Обмеження

- External GitHub API limit:
  60 запитів/годину(без автентифікації) або 5000 запитів/годину(з автентифікацією)
- Бюджет: 0$ (пет проект)

## 2. Оцінка навантаження

- Активні користувачі: 10
- Підписки на користувача: 1-5

## 3. High-level Architecture

Система складається з трьох сервісів додатку та спільної бази даних:

- **Subscription API** (`app`) — публічне HTTP API (`/api/*`), внутрішнє API для сканера (`/internal/scanner/*`), опційний gRPC, доступ до PostgreSQL
- **Release Scanner** (`release-scanner`) — фоновий воркер без доступу до БД; отримує цілі сканування та оновлює `lastSeenTag` через внутрішнє HTTP API або gRPC
- **Notification Worker** (`notification`) — обробляє чергу BullMQ та надсилає email

Детальна архітектурна документація (англійською): [`docs/architecture/overview.md`](../architecture/overview.md), [`layers.md`](../architecture/layers.md).

Комунікація між сервісами — синхронний HTTP з автентифікацією `INTERNAL_API_KEY`.

![img.png](./images/high_level_diagram.png)

Детальніше: [`ADR-004`](../ADR/ADR-004-release-scanner-microservice.md)

## 4. API endpoints

Усі роути префіксовані з використанням `/api`.

| Метод | Ендпоїнт                       | Опис                                                                             |
| ----- | ------------------------------ | -------------------------------------------------------------------------------- |
| POST  | `/subscribe`                   | Підписка на розсилку повідомлень про нові версії для певного репозиторію GitHub. |
| GET   | `/confirm/{token}`             | Підтведження підписки.                                                           |
| POST  | `/unsubscribe/{token}`         | Відписка від розсилки сповіщень про реліз репозиторію.                           |
| GET   | `/subscriptions?email={email}` | Отримання усіх активних підписок для певної поштової скриньки.                   |

### Internal scanner API (`/internal/scanner`)

Використовується лише сервісом `release-scanner`. Захищено `INTERNAL_API_KEY`.

| Метод | Ендпоїнт | Опис |
| ----- | -------- | ---- |
| GET | `/scan-targets` | Список репозиторіїв з підтвердженими підписками для сканування |
| PATCH | `/repositories/{id}/last-seen-tag` | Оновлення `lastSeenTag` з optimistic locking |

Для детальнішої інформації відкрийте [`схему swagger`](../swagger.yaml)

### DB schema

Схема ДБ у [`ADR-002`](../ADR/ADR-002-database.md)

## 5. Тестування системи

Система покрита лише юніт тестами. Також планується покриття інтеграційними тестами

#### Unit тести

Покрито усі критично важливі компоненти системи які відповідають за бізнес логіку. Усі залежності замокані(ДБ, сторонні API і т.д.)

## 6. Observability (Cпостережуваність)

### Логування

**Інструменти**: Вбудований fastify logger

**Рівні логування**:

- `INFO` - важливі події (запуск серверу, підписка користувача, запуск сканування)
- `ERROR` - помилки та винятки
- `DEBUG` - детальна інформація для розробки

### Метрики

**Ключові метрики**:

- http_requests_total (Counter: метод, шлях, статус-код)
- http_request_duration_seconds (Histogram: метод, шлях, статус-код)

**Системні метрики**

- CPU Usage (користувацький та системний час)
- Memory Usage (Heap, RSS, External)
- Event Loop Lag (затримка циклу подій)
- Active Handles / Requests
- Garbage Collection (тривалість та частота)
